# 语保词句

浏览和查询中国语言资源保护工程（语保）的词汇与语法例句数据，支持地图、卡片、表格三种展示模式。

数据来源：共 1000 条词汇数据，覆盖 1247 个方言点（所有数据均来自语保官方平台，对登录用户公开）。

## 词汇查询

### 基本操作

1. 在输入框搜索条目（支持繁简匹配），从下拉建议中点选
2. 选择展示模式：地图、卡片或表格

### 地图模式

- 比例尺较小时自动聚合显示方言点
- 放大地图后显示具体内容
- 三种显示方式：**释义（文字）**、**语音（发音）**、**地名**
- 点击地图上的点可弹窗查看详情
- 支持全屏显示

### 卡片模式

- 卡片显示地名、词汇、注释、发音、分区
- 搜索框可输入地名/注释/音标进行匹配
- 支持无限滚动加载

### 表格模式

- 所有列支持点击排序
- 部分列支持弹窗筛选（多选、反选）
- 支持全屏查看
- 表格中可搜索匹配

## 语法查询（语保 50 句）

切换到「语法」标签页，可查询语保工程的 50 个语法例句：

- 同样支持卡片、表格、地图三种模式
- 卡片显示地名、具体发音、注释文字与分区
- 表格支持搜索、排序、筛选
- 地图模式因句子较长，显示可能不够美观但保留功能

## 提示

- 只输入半个词或自由输入未在清單中的内容时表格不会出现
- 词彙与语法各有自己的输入框，切换标签页后需重新输入
- 移动端做了专门适配，手机可正常使用
